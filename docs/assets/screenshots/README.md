# Screenshot files for the ClipTap website

Add screenshots with these filenames:

- `helper-dashboard.png` — ClipTap Helper dashboard with server status, dependency checks, active download queue, download defaults, and recent logs.
- `player-panel.png` — ClipTap panel inside the YouTube player with start/end controls, loop, section download, and full download.
- `timeline-handles.png` — YouTube timeline with the blue start handle and orange end handle.
- `helper-queue.png` — ClipTap Helper queue page with platform, format, progress, and status columns.
- `helper-settings.png` — ClipTap Helper settings page with download defaults.
